# Fingerprint registry

Per-user record of shapes already built. A new build must differ from every row
below on at least 4 of 6 dimensions: grammar, nav treatment, hero device,
act-sequence shape, close pattern, signature move.

| # | Build | Grammar | Nav | Hero device | Act-sequence shape | Close | Signature move | World | Port |
|---|---|---|---|---|---|---|---|---|---|
| 1 | kniff-home | Workbench (new, named) | fixed instrument HUD bottom-left (object name + spec, doubles as jump-nav) + thin mono top bar | pinned WebGL gear-reduction unit, assembles on load, pointer-steered key light, drag to turn/explode, material switch regrades page accent | pin(hero/peak 2.8) → pin+reveal(process 3.0) → pan+tilt(referenzen 3.0) → flow(origin) → pan+tilt(sortiment 2.6) → pin close(1.25); ~12.7vh, 6 acts, span lopsided (hero + origin carry the page) | pinned "empty bench" ground, one-value hold cues, magnetic CTA "Projekt einreichen", full footer inside the stage | one pointer-steered workshop key light that rotates + shades **every** printed object on the page (hero WebGL + CSS-3D rail cubes + glows); scroll-velocity drives it on touch | low-key cinematic, Interstellar grade, matte PLA on dark anodised aluminium | static (kniff.shop / Netlify) |
